{% extends 'base.html.twig' %}

{% block body %}
<section class="articles">
    <div class="content">
        <h3>{{ article.title }}</h3>
        <img src="{{ article.image }}">
        {{ article.content | raw }}
    </div>
</section>
{% endblock %}